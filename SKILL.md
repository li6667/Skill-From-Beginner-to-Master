---
name: systematic-thinking
description: 用“最小方案→暴露问题→引入机制→利用特性→验证边界”的问题驱动方法，帮助理解陌生代码库、Agent Runtime、并发、协议和架构，并把源码事实整理成可复述的面试答案。
version: "3.0"
author: paicli
tags: [learning, codebase, onboarding, architecture, interview, java, agent]
---

# Systematic Thinking

## 目标

帮助用户把“会背概念但不会落到代码”的知识，转化为可追踪、可验证、可复述的系统理解。

核心原则：

> 先找最简单的实现，再问它为什么不够；每增加一层，都要说明它利用了什么特性、解决了什么问题，又引入了什么代价。

不要从术语百科或类名清单开始。优先建立真实调用链，再用用户已经学过的 Java、JUC、操作系统和计算机网络知识推导陌生部分。

## 何时使用

- 面对陌生项目、源码或复杂模块，不知道从哪里开始；
- 能背 Future、线程池、TCP、Agent 等定义，但无法解释它们在项目中为何存在；
- 询问两个组件为什么同时存在，或“这一层是不是多余”；
- 需要把源码实现整理成面试中的架构说明；
- 需要理解 Agent、Tool Calling、MCP、DAG、Memory、RAG 等跨层系统；
- 需要从一次真实调用追到协议、线程、进程和数据结构。

不要把它用于只需要查一个 API 用法、生成简单代码或回答独立事实的问题。

## 总体工作流

### 1. 先确认事实

阅读入口、调用方和实现，区分代码已经实现的能力、文档中的计划能力和未来建议。涉及项目问题时优先使用 `rg`、调用链和测试验证，不把类名、注释或 README 自动当作真实行为。

### 2. 找到最小主路径

用不超过 10 个节点画一次完整数据流：

```text
用户输入 → 入口解析 → 核心对象 → 请求/任务构造 → 外部调用或本地执行 → 结果回灌 → 最终输出
```

先不要加入异常、并发和优化分支。主路径不清楚时，不要跳到高级术语。

### 3. 按问题驱动推导

对每个难点严格按以下模板展开：

```text
最简单方案：如果只用已经学过的基础能力，会怎么实现？
暴露问题：在并发、跨进程、超时、扩展性或一致性场景下哪里会失败？
加入哪一层：引入什么协议、数据结构、中间件或调度器？
利用什么特性：它提供了哪项关键能力？
解决什么问题：原来的失败点如何被覆盖？
代价与边界：它没有解决什么？增加了什么复杂度？
```

不要只说“用了 CompletableFuture”“用了 MCP”“用了线程池”。必须解释这些东西为什么在这条链路上出现。

### 4. 把概念放回代码

每个抽象至少回答：谁创建、谁调用、持有什么状态、结果交给谁。遇到并发时补充：进程/线程/结果对象的区别、线程私有与共享状态、发送/完成/收集顺序，以及 ID、Future、队列或锁的作用。

### 5. 最后讲取舍

说明为什么不用更简单方案、为什么没有直接使用成熟框架、当前实现边界以及生产化演进方向。不要把推测说成事实，也不要虚构指标、锁、缓存、沙箱或自动路由。

## 常用解释模板

```text
这个模块要解决的问题是：...
最简单的做法是：...，但会出现：...
所以增加了：...，它利用：...
最终实现：...

代码落点：创建方...；调用方...；输入...；输出...；状态/线程边界...
代价和边界：...
```

并发解释要明确：

```text
ExecutorService = 执行 Java 任务的线程池
Future/CompletableFuture = 未来结果的凭证/容器
BlockingQueue = 任务队列或对象池，具体看泛型和调用位置
ConcurrentHashMap = 并发 Map，不等于取出的对象和副作用线程安全
```

## Agent / MCP 特别规则

```text
LLM：产生文本或结构化 Tool Call，不直接执行 Java 代码
Agent：维护消息历史并运行 ReAct 循环
Orchestrator：确定性推进任务、依赖、重试和汇总
ToolRegistry：找到本地 Tool 或 MCP Bridge
MCP Client：处理 initialize、tools/list、tools/call 等业务语义
JSON-RPC Client：处理 Request、Response、Notification、ID 和 pending Future
Transport：通过 stdio/HTTP 传输 JSON
MCP Server：执行外部工具
```

Tool Calling 是 LLM ↔ Runtime 的协议；MCP 是 Runtime ↔ 外部 Tool Server 的协议。不要混淆 LLM Tool Call ID 与 JSON-RPC Request ID。

解释 MCP 的 Future 时，必须采用这条因果链：

```text
每个请求线程读取 stdout → 多线程竞争共享输入流、响应可能错配
→ 单一 stdout reader → requestId 解复用
→ pending Map 找到等待对象 → CompletableFuture.complete 唤醒请求线程
```

明确：Future 不是线程，CompletableFuture 也不是跨进程对象；跨进程的是 stdin/stdout/HTTP 数据。

如果通知处理器需要再次发起 RPC，说明为什么不能在 stdout reader 线程中同步等待，否则会出现读取线程等待自己响应的死锁。

## 面试输出顺序

先给 60～120 秒版本，再按追问展开：

1. 工程问题；
2. 总体架构；
3. 一次真实数据流；
4. 关键设计与取舍；
5. 异常、并发和边界；
6. 结果或验证方式。

## 事实纪律

- 代码事实优先于文档描述，必要时给出文件和方法位置。
- 不把局部能力说成完整协议、生产级平台或强隔离系统。
- 不把有缓存统计说成每次必然命中，不把有线程池说成所有副作用线程安全。
- 没有真实指标时使用可验证措辞，不编造百分比。
- 用户要求面试话术时，优先给真实、可防守的表述，不虚构代码能力。

## 推荐输出结构

1. 一句话结论；
2. 结构图或调用链；
3. 最简单方案 → 问题 → 引入机制 → 解决方案；
4. 代码中的真实落点；
5. 设计取舍和边界；
6. 面试可直接复述的版本；
7. 仍需核对的事实。
